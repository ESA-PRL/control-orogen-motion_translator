# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(motion_translatorTaskLib)
ADD_LIBRARY(${MOTION_TRANSLATOR_TASKLIB_NAME} SHARED 
    ${MOTION_TRANSLATOR_TASKLIB_SOURCES})
add_dependencies(${MOTION_TRANSLATOR_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${MOTION_TRANSLATOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${MOTION_TRANSLATOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MOTION_TRANSLATOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MOTION_TRANSLATOR_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${MOTION_TRANSLATOR_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${MOTION_TRANSLATOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MOTION_TRANSLATOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MOTION_TRANSLATOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/motion_translator)

